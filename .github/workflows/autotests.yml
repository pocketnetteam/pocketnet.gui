name: Launch Tests

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: autotest

    steps:
      - name: Start
        run: |
          cd C:\actions-runner\autotest\
          .\AutomaticTesting.exe -all

